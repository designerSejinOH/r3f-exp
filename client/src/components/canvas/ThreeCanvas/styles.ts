import styled from 'styled-components'
import tw from 'twin.macro'

export const ThreeCanvas = styled.div`
  ${tw`
    w-full
    h-full
    flex
    flex-col
    justify-center
    items-center
    bg-transparent
    `}
`
